'use client';
import {useState} from 'react';
export default function Unsubscribe(){const[status,setStatus]=useState('idle');const[error,setError]=useState('');async function remove(){setStatus('loading');try{const token=new URLSearchParams(window.location.search).get('token');const response=await fetch('/api/unsubscribe',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})});const result=await response.json();if(!response.ok)throw new Error(result.error);setStatus('success');}catch(e){setStatus('error');setError(e instanceof Error?e.message:'Please try again.');}}return <main className="privacy"><a className="logo" href="/">aevra</a><h1>A little less in your inbox.</h1>{status==='success'?<p role="status">You’ve been removed from our waitlist and your email has been deleted.</p>:<><p>Use your private link to unsubscribe and permanently delete your email from the aevra waitlist.</p><button onClick={remove} disabled={status==='loading'}>{status==='loading'?'Removing…':'Unsubscribe & delete my email'}</button>{error&&<p role="alert">{error}</p>}</>}<p><a href="/">Back to aevra</a></p></main>}
